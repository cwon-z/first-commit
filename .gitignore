# This project has no build step and no dependencies — the only things that
# should ever appear here are editor and OS droppings.
node_modules/
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
drafts/.candidates/

# The server's data file: real accounts and real progress. Never committed.
data/
*.json.tmp

# Kept out of the repository on purpose:
#   the design canvas export the interface was built from — 1.4 MB, not source
#   and a scratch file that predates the rebuild
First Commit - offline.html
test.html

# Deployment configuration: the examples in deploy/ are committed, the real
# app.env never is — it holds the SMTP credential.
app.env
deploy/app.env
